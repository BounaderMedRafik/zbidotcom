import React from "react";
import { result } from "./tmnyika";

const ChatBot = () => {
  const resultat = "hello";

  console.log(resultat);
  console.log(result);
  return <div>ChatBot</div>;
};

export default ChatBot;
